# Heavy / ephemeral benchmark data — never commit (gigabytes of repo clones,
# worktrees, fresh-DB artifacts, and per-run logs/results).
repos/
worktrees/
cacheval/
golden/results/
golden/work/
gauntlet/RG*/
gauntlet/SUMMARY.md
*.log

# Long-horizon benchmark: secrets + run artifacts — NEVER commit
longhorizon/.secrets/
longhorizon/reference/node_modules/
longhorizon/acceptance/node_modules/
longhorizon/runs/
longhorizon/work/
